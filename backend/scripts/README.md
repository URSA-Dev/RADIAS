# backend/scripts

**Project:** RADIAS · **Mirrors:** SSP-Vercel-Build/backend/scripts

## Purpose

Demo runners. Presentation and orchestration only — the logic lives in `src/`.

## Contents

| Item | Type | Purpose |
| --- | --- | --- |
| `act2-butler-snow.ts` | Script | Act 2 — public-record scan with citations (`npm run act2`) |
| `act3-hcb-variance.ts` | Script | Act 3 — reconciliation, DVI, question pack (`npm run act3`) |

Both accept `--json` to emit the field shapes `index.html` already renders.
