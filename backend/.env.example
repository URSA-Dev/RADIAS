# Copy to .env and fill in. .env is gitignored.
#
# Not needed if you have run `ant auth login` — the SDK picks up that profile
# automatically and a bare `new Anthropic()` works with no env var set.
ANTHROPIC_API_KEY=sk-ant-...
